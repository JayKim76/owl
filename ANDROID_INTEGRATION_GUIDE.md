# 🦉 Owl Leak - 초보자를 위한 안드로이드 앱 연결 가이드

안녕! 이 문서는 우리가 만든 웹사이트 정보를 안드로이드 스마트폰 앱에서도 볼 수 있게 연결하는 방법을 아주 쉽게 설명한 가이드야. 마치 **"스마트폰(앱)이 서버(웹사이트)에게 말을 거는 방법"**을 배우는 과정이라고 생각하면 돼!

---

## 🏗️ 1단계: 서버(웹사이트)가 대답할 준비 하기

우선, 서버가 스마트폰 앱의 질문에 대답할 수 있게 문을 열어줘야 해.

### 1.1 "API"가 뭐야? (식당의 웨이터)
스마트폰 앱이 데이터베이스에 직접 들어가서 정보를 꺼내오는 건 위험해. 그래서 **API**라는 '웨이터'를 통해 정보를 요청해야 해.
- **손님(앱)**: "고객 목록 좀 보여주세요!"
- **웨이터(API)**: "네, 잠시만요. 주방(데이터베이스)에서 가져다 드릴게요!"

### 1.2 서버 실행하기
컴퓨터에서 서버를 켤 때, 다른 기기에서도 접속할 수 있게 아래 명령어를 입력해.
```bash
npm run dev -- -H 0.0.0.0
```
> [!TIP]
> **왜 0.0.0.0 인가요?** 보통은 '나 혼자'만 보려고 서버를 켜지만, 이렇게 하면 '내 주변 친구들(같은 와이파이)'도 접속할 수 있게 문을 활짝 여는 거야!

---

## 📱 2단계: 안드로이드 앱에 "전화기" 설치하기

스마트폰 앱이 서버에 전화를 걸려면 '전화기' 역할을 하는 프로그램이 필요해. 안드로이드에서는 이걸 **Retrofit(레트로핏)**이라고 불러.

### 2.1 앱 설정 파일에 도구 추가하기 (`build.gradle.kts`)
앱의 '가방'에 필요한 도구들을 챙겨 넣는 거야.
```kotlin
dependencies {
    // 서버와 통신하는 전화기 (Retrofit)
    implementation("com.squareup.retrofit2:retrofit:2.9.0")
    // 서버의 말을 우리말로 번역해주는 번역기 (Gson)
    implementation("com.squareup.retrofit2:converter-gson:2.9.0")
}
```

### 2.2 "인터넷 써도 돼요?" 허락받기 (`AndroidManifest.xml`)
스마트폰은 주인 허락 없이 인터넷을 쓰지 않아. 그래서 미리 허락을 받아야 해.
```xml
<uses-permission android:name="android.permission.INTERNET" />
```

---

## 📝 3단계: 어떤 말을 주고받을지 약속하기

서버와 앱이 서로 대화할 때 똑같은 단어를 써야 헷갈리지 않겠지?

### 3.1 "고객 정보"라는 박스 만들기 (데이터 클래스)
이름, 전화번호, 주소 같은 정보를 담을 '정리함'을 만드는 거야.
```kotlin
data class Customer(
    val name: String,   // 이름
    val phone: String,  // 전화번호
    val region: String  // 사는 지역
)
```

### 3.2 "메뉴판" 만들기 (인터페이스)
앱이 서버에게 시킬 수 있는 심부름 목록이야.
```kotlin
interface OwlApiService {
    @GET("api/customers") // "고객 목록 좀 가져와!"
    suspend fun getCustomers(): List<Customer>

    @POST("api/customers") // "새로운 고객 등록해줘!"
    suspend fun registerCustomer(@Body customer: Customer): Customer
}
```

---

## 🛠️ 4단계: 고장 나지 않게 관리하기 (유지보수)

앱을 만들고 나서도 계속 돌보아줘야 해.

### 4.1 데이터베이스 구조 바꾸기 (Prisma)
주방의 냉장고 구조를 바꾸는 것과 같아. 냉장고 칸을 늘렸다면 웨이터에게도 알려줘야 해.
```bash
npx prisma migrate dev --name add_new_field
npx prisma generate
```

### 4.2 VIP 카드 만들기 (JWT 보안)
아무나 우리 서버에 들어와서 정보를 빼가면 안 되잖아? 그래서 로그인한 사람에게만 'VIP 카드(토큰)'를 주고, 앱은 매번 이 카드를 보여주며 정보를 요청해야 해.

---

## 💡 마지막 꿀팁!
- **서버 주소 주의!**: 컴퓨터에서 테스트할 때 안드로이드 앱은 서버 주소를 `127.0.0.1`이 아니라 `10.0.2.2`라고 불러야 해. (에뮬레이터가 쓰는 특별한 별명이야!)
- **Prisma Studio**: `npx prisma studio`를 입력하면 데이터베이스를 마치 엑셀 화면처럼 편하게 볼 수 있어. 공부할 때 아주 유용해!

어때? 생각보다 복잡하지 않지? 하나씩 차근차근 따라 해봐! 🦉✨
